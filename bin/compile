#!/bin/sh

##
# usage: bin/compile <build-dir> <cache-dir>

set -e

mkdir -p "$1" "$2"
build=$(cd "$1/" && pwd)
cache=$(cd "$2/" && pwd)
ver=release.r60.3

echo "-----> Using Go $ver"

mkdir -p $cache/go-$ver
(
    set -e
    cd $cache/go-$ver
    test -d go && exit

    if ! test -f go-$ver.tar.gz
    then
        rm -rf $cache/* # be sure not to build up cruft
        echo "-----> Fetching Go $ver"
        curl -sO https://s3.amazonaws.com/golang/go-$ver.tar.gz
    fi

    tar zxf go-$ver.tar.gz
    rm -f go-$ver.tar.gz
)

rm -f /tmp/go
ln -s $cache/go-$ver/go /tmp/go

GOROOT=/tmp/go
export GOROOT
PATH=$GOROOT/bin:$PATH

cd $build

GOPATH=`pwd`
export GOPATH
unset GIT_DIR # unset git dir or it will mess with goinstall
echo "-----> Running goinstall"
for f in $(find src -type f | egrep "\.go$" | sort | uniq)
do goinstall ./$(dirname $f)
done
